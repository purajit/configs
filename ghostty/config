term = xterm-256color
auto-update = check
auto-update-channel = tip

# APPEARANCE
macos-titlebar-style = hidden

window-theme = system
theme = light:deep-theme-light,dark:deep-theme-dark

window-decoration = true
window-save-state = never
# large values which ghostty will clamp and result in a maximized window
window-height = 500
window-width = 1600

font-family = Mononoki Nerd Font Propo
font-size = 16

# BEHAVIOR
copy-on-select = clipboard
macos-option-as-alt = true
mouse-shift-capture = true
shell-integration = zsh

# QUICK TERMINAL
quick-terminal-animation-duration = 0
# I prefer my Hammerspoon-controlled quick terminal instead
# keybind = global:cmd+grave_accent=toggle_quick_terminal
